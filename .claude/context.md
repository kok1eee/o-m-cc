# Context

> compaction で失われる文脈を保存。compaction summary と合わせて復元に使用。
> Learnings に長期的価値があれば MEMORY.md に反映すること。

### Snapshot (03/12 13:18, end)

**Intent:** settings

**Context:**

前回のセッションの続きですね。どの settings を確認・変更したいですか？

1. **プラグイン設定** — `.claude-plugin/plugin.json`, `.claude-plugin/settings.json`
2. **Claude Code 設定** — `~/.claude/settings.json`（グローバル）/ `.claude/settings.json`（プロジェクト）
3. **Hooks 設定** — `.claude/hooks.json`
4. **その他** — 具体的に教えてください
